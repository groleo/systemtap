#! stap -p2

# special case of multiline macro parsing
# note macro signature spills over multiple lines
@define kaprobe(
        a)

%(
        probe @a {
              foo = 2+2
              print(foo)
        }
%)   

@kaprobe(begin)
